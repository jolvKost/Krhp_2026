Validation error(s) at /visualStyles/card/*/categoryLabels/0/color: must be object (type; matching schema #/oneOf/0/type); must be object (type; matching schema #/oneOf/1/type); must be object (type; matching schema #/oneOf/2/type); must match exactly one schema in oneOf (oneOf; matching schema #/oneOf)
Validation error(s) at /visualStyles/card/*/labels/0/color: must be object (type; matching schema #/oneOf/0/type); must be object (type; matching schema #/oneOf/1/type); must be object (type; matching schema #/oneOf/2/type); must match exactly one schema in oneOf (oneOf; matching schema #/oneOf)
Validation error(s) at /visualStyles/slicer/*/header/0/background: must be object (type; matching schema #/oneOf/0/type); must be object (type; matching schema #/oneOf/1/type); must be object (type; matching schema #/oneOf/2/type); must match exactly one schema in oneOf (oneOf; matching schema #/oneOf)
Validation error(s) at /visualStyles/slicer/*/header/0/fontColor: must be object (type; matching schema #/oneOf/0/type); must be object (type; matching schema #/oneOf/1/type); must be object (type; matching schema #/oneOf/2/type); must match exactly one schema in oneOf (oneOf; matching schema #/oneOf)
Validation error(s) at /visualStyles/slicer/*/items/0/fontColor: must be object (type; matching schema #/oneOf/0/type); must be object (type; matching schema #/oneOf/1/type); must be object (type; matching schema #/oneOf/2/type); must match exactly one schema in oneOf (oneOf; matching schema #/oneOf)
Validation error(s) at /visualStyles/tableEx/*/columnHeaders/0/backColor: must be object (type; matching schema #/oneOf/0/type); must be object (type; matching schema #/oneOf/1/type); must be object (type; matching schema #/oneOf/2/type); must match exactly one schema in oneOf (oneOf; matching schema #/oneOf)
Validation error(s) at /visualStyles/tableEx/*/columnHeaders/0/fontColor: must be object (type; matching schema #/oneOf/0/type); must be object (type; matching schema #/oneOf/1/type); must be object (type; matching schema #/oneOf/2/type); must match exactly one schema in oneOf (oneOf; matching schema #/oneOf)
Validation error(s) at /visualStyles/tableEx/*/total/0/backColor: must be object (type; matching schema #/oneOf/0/type); must be object (type; matching schema #/oneOf/1/type); must be object (type; matching schema #/oneOf/2/type); must match exactly one schema in oneOf (oneOf; matching schema #/oneOf)
Validation error(s) at /visualStyles/tableEx/*/total/0/fontColor: must be object (type; matching schema #/oneOf/0/type); must be object (type; matching schema #/oneOf/1/type); must be object (type; matching schema #/oneOf/2/type); must match exactly one schema in oneOf (oneOf; matching schema #/oneOf)
Validation error(s) at /visualStyles/tableEx/*/values/0/backColor: must be object (type; matching schema #/oneOf/0/type); must be object (type; matching schema #/oneOf/1/type); must be object (type; matching schema #/oneOf/2/type); must match exactly one schema in oneOf (oneOf; matching schema #/oneOf)
Validation error(s) at /visualStyles/tableEx/*/values/0/backColorPrimary: must be object (type; matching schema #/oneOf/0/type); must be object (type; matching schema #/oneOf/1/type); must be object (type; matching schema #/oneOf/2/type); must match exactly one schema in oneOf (oneOf; matching schema #/oneOf)
Validation error(s) at /visualStyles/tableEx/*/values/0/fontColor: must be object (type; matching schema #/oneOf/0/type); must be object (type; matching schema #/oneOf/1/type); must be object (type; matching schema #/oneOf/2/type); must match exactly one schema in oneOf (oneOf; matching schema #/oneOf)
Validation error(s) at /visualStyles/pivotTable/*/columnHeaders/0/backColor: must be object (type; matching schema #/oneOf/0/type); must be object (type; matching schema #/oneOf/1/type); must be object (type; matching schema #/oneOf/2/type); must match exactly one schema in oneOf (oneOf; matching schema #/oneOf)
Validation error(s) at /visualStyles/pivotTable/*/columnHeaders/0/fontColor: must be object (type; matching schema #/oneOf/0/type); must be object (type; matching schema #/oneOf/1/type); must be object (type; matching schema #/oneOf/2/type); must match exactly one schema in oneOf (oneOf; matching schema #/oneOf)
Validation error(s) at /visualStyles/pivotTable/*/values/0/backColor: must be object (type; matching schema #/oneOf/0/type); must be object (type; matching schema #/oneOf/1/type); must be object (type; matching schema #/oneOf/2/type); must match exactly one schema in oneOf (oneOf; matching schema #/oneOf)
Validation error(s) at /visualStyles/pivotTable/*/values/0/fontColor: must be object (type; matching schema #/oneOf/0/type); must be object (type; matching schema #/oneOf/1/type); must be object (type; matching schema #/oneOf/2/type); must match exactly one schema in oneOf (oneOf; matching schema #/oneOf)